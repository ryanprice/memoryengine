# Copy to .env and fill in your keys
# Memory Engine will load this automatically via python-dotenv

# ── API Keys ──────────────────────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ── Override config.yaml values at runtime (optional) ─────
# MEMORY_DIR=memory
# PROVIDER=openai
# MODEL=gpt-4o-mini
